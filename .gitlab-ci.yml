image: python:3.7

test:
  coverage: '/TOTAL[\s|\d]+(\d+.\d+%)/'
  script:
    - apt update && apt install -y python3-pip
    - python3 -m pip install pip pytest pytest-cov
    - python3 -m pytest --cov=tests
